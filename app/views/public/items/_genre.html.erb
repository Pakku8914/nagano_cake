<div>
  <h1>ジャンル一覧</h1>
  <% genre.each do |f| %>
    <%= form_with url: items_path, method: :get, local: true do |form| %>
      <%= form.submit :genre, value: f.name %>
    <% end %>
  <% end %>
</div>